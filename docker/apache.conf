<VirtualHost *:80>
    DocumentRoot /var/www/html/public

    # Allow long-running SSE streams (cloud image deployments can take 30+ minutes)
    Timeout 3600

    # Allow large file uploads (e.g. Windows ISOs)
    php_value upload_max_filesize 20G
    php_value post_max_size 20G
    php_value max_execution_time 3600
    php_value max_input_time 3600

    <Directory /var/www/html/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # Security headers (supplement .htaccess)
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "DENY"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
